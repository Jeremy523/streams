// Local dependencies
var About = require('./about');
var Home = require('./home'); //file extension unnecessary
var Search = require('./search');

module.exports = {
    About: About,
    Home: Home,
    Search: Search
};