// Local dependencies
var About = require('./about');
var Home = require('./home'); //file extension unnecessary
var Users = require('./users');

module.exports = {
    About: About,
    Home: Home,
    Users: Users
};